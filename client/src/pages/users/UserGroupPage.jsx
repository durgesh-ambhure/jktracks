import SkeletonPage from '../common/SkeletonPage';

export default function UserGroupPage() {
  return (
    <SkeletonPage
      title="User Group"
      note="User Group management (creating custom groups beyond the 5 seeded roles) is a Tier-2 placeholder. Group Rights currently operates on the fixed role set — see Group Rights page."
    />
  );
}
