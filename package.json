{
  "name": "courier-management-system",
  "private": true,
  "version": "1.0.0",
  "description": "Independent courier/shipping management admin panel (MERN)",
  "scripts": {
    "dev": "concurrently -k -n SERVER,CLIENT -c blue,green \"npm run server\" \"npm run client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "seed": "npm run seed --prefix server",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "format": "npm run format --prefix client && npm run format --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
